apiVersion: v1
kind: Service
metadata:
  name: main
  labels:
    app: acmeair
spec:
  selector:
    app: acmeair
    tier: main
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

